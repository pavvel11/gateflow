meta {
  name: Get Product
  type: http
  seq: 2
}

get {
  url: {{BASE_URL}}/api/v1/products/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{API_KEY}}
}

vars:pre-request {
  id: PRODUCT_ID_HERE
}
