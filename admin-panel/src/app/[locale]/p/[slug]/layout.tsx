export const metadata = {
  title: 'Product Page',
  description: 'Purchase or download product',
};

export default function ProductLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
